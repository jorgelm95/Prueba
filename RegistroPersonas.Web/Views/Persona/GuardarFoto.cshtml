@model RegistroPersonas.Dominio.Foto

@{
    ViewBag.Title = "GuardarFoto";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>GuardarFoto</h2>


<section class="panel panel-default">
    <div class="panel-heading">
        <strong>Registro Detalle</strong>
    </div>
    <div class="panel-body">



        @using (Html.BeginForm(actionName: "GuardarFoto", controllerName: "Persona", routeValues: new { id = 1 }, method: FormMethod.Post))
        {
            <div class="form-group">
                <label>Agregar Foto</label>
                <input type="file" name="imagen" value="" id="imagen" class="form-control" />
                <div class="form-group">
                    <label>Posicion foto</label>
                    <select id="ubicacion" name="ubicacion">
                        <option value="frontal">Frontal</option>
                        <option value="lateral derecho">Lateral derecha</option>
                        <option value="lateral izquierda">Lateral izquierda</option>
                    </select>
                </div>

                <div class="form-group">
                    <label>Breve descripcion de su foto</label>
                    <textarea name="Descripciion" style="width:460px; height:120px;"></textarea>
                </div>
                <div class="form-group">
                    <input type="submit" name="subir" value="Subir imagen" id="subir" />
                </div>
            </div>
        }
    </div>
</section>



